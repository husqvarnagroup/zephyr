config SOC_FAMILY_SIM3
	bool
	select HAS_SEGGER_RTT
	select BUILD_OUTPUT_HEX

if SOC_FAMILY_SIM3

config SOC_FAMILY
	string
	default "silabs_sim3"

config SOC_SIM3_FAST_CLOCK
	bool "Enable fast clock speeds"

source "soc/arm/silabs_sim3/*/Kconfig.soc"

endif # SOC_FAMILY_SIM3
